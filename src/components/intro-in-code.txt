<!DOCTYPE html>
<html lang='en'>
<head>
  <title>Zack's Portfolio</title>
</head>
<body>

  <h1>Looking for a developer?</h1>

  <p>
    Moin moin! I am a frontend developer from London living in Hamburg.
    I currently work for ABOUT YOU GmbH within their Cloud division
    developing the future of Content Management Systems. POW!
  </p>

  <script type='text/javascript'>

    const languages = [
      'JavaScript / TypeScript', 
      'HTML & CSS',
      'Python',
      'Golang'
    ];

    const frameworks = [
      'React JS / React Native',
      'Vue JS'
    ];

    const libraries = [
      'Webpack',
      'Styled Components',
      'Redux / MobX',
      'Cypress',
      'Jest',
      'Enzyme / React Testing Library'
    ];

    const mySkills = [...languages, ...frameworks, ...libraries];

    const ul = document.createElement('ul');
    ul.id = 'skills';

    mySkills.map((skill) => {
      const li = document.createElement('li');
      li.appendChild(document.createTextNode(skill));
      ul.appendChild(li);
    });

    document.body.appendChild(ul);

  </script>
  
</body>
</html>